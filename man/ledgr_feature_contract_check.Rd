% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-inspection.R
\name{ledgr_feature_contract_check}
\alias{ledgr_feature_contract_check}
\title{Check feature warmup feasibility against a snapshot}
\usage{
ledgr_feature_contract_check(snapshot, features)
}
\arguments{
\item{snapshot}{A sealed \code{ledgr_snapshot}.}

\item{features}{A \code{ledgr_feature_map}, a list of \code{ledgr_indicator} objects,
or one \code{ledgr_indicator}.}
}
\value{
A tibble with one row per \code{(instrument_id, feature_id)} pair. It
includes \code{alias}, \code{instrument_id}, \code{feature_id}, \code{source},
\code{requires_bars}, \code{stable_after}, \code{available_bars}, and
\code{warmup_achievable}. \code{warmup_achievable} is \code{TRUE} when the instrument has
at least \code{stable_after} bars.
}
\description{
\code{ledgr_feature_contract_check()} joins declared feature contracts to the
actual per-instrument bar counts in a sealed snapshot. It is a pre-run
inspection helper: it tells you whether each feature can become usable for
each instrument in this snapshot. It does not compute feature values and does
not mutate the snapshot.
}
\details{
Feature factories of the form \code{function(params)} are intentionally rejected
here because the helper cannot know which concrete parameter set to use.
Materialize the factory first, then pass the resulting indicators or feature
map. The error class is \code{ledgr_feature_factory_requires_params}.
}
\section{Articles}{

Indicators, feature IDs, and pulse feature views:

\code{vignette("indicators", package = "ledgr")}
\code{system.file("doc", "indicators.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = ledgr_utc("2020-01-01") + 86400 * 0:4,
  instrument_id = "AAA",
  open = 100:104,
  high = 101:105,
  low = 99:103,
  close = 100:104,
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
features <- list(ledgr_ind_returns(3), ledgr_ind_sma(10))
ledgr_feature_contract_check(snapshot, features)
ledgr_snapshot_close(snapshot)

}

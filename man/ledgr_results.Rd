% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{ledgr_results}
\alias{ledgr_results}
\title{Extract ledgr result tables}
\usage{
ledgr_results(bt, what = c("equity", "fills", "trades", "ledger"))
}
\arguments{
\item{bt}{A \code{ledgr_backtest} object.}

\item{what}{Result table to extract: \code{"equity"}, \code{"fills"}, \code{"trades"}, or
\code{"ledger"}.}
}
\value{
A ledgr result table, which is a classed tibble with the requested
result columns.
}
\description{
Package-prefixed convenience wrapper around \code{tibble::as_tibble()} for
backtest result tables.
}
\details{
The returned object is tibble-compatible. Its print method may compact
all-midnight UTC timestamps for EOD output according to
\code{options(ledgr.print_ts_utc)}, but programmatic access keeps \code{ts_utc} as
POSIXct UTC.

\code{what = "fills"} returns execution fill rows, including opening and closing
actions. Fill rows include execution \code{side}, absolute \code{qty}, \code{price}, \code{fee},
derived \code{action}, and \code{realized_pnl}. Opening fills have \code{action = "OPEN"}
and do not count as closed trades.

\code{what = "trades"} returns closed trade rows only. This table has the same
zero-row schema as fills, but only rows with \code{action = "CLOSE"} are present.
It is the source for \code{n_trades}, \code{win_rate}, and \code{avg_trade}.

\code{what = "equity"} returns the public equity curve used for return,
drawdown, volatility, and exposure metrics. Open positions can affect equity
through \code{positions_value} even when there are zero closed trade rows.
}
\section{Articles}{

Metrics and accounting:
\code{vignette("metrics-and-accounting", package = "ledgr")}
\code{system.file("doc", "metrics-and-accounting.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:3,
  instrument_id = "AAA",
  open = c(100, 101, 102, 103),
  high = c(101, 102, 103, 104),
  low = c(99, 100, 101, 102),
  close = c(100, 101, 102, 103),
  volume = 1000
)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- 1
  targets
}
bt <- ledgr_backtest(data = bars, strategy = strategy, initial_cash = 1000)
ledgr_results(bt, what = "trades")
close(bt)
}

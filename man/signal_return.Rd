% Generated by roxygen2: do not edit by hand
\name{signal_return}
\alias{signal_return}
\title{Build a return signal from registered return features}
\usage{
signal_return(ctx, lookback = 20L)
}
\arguments{
\item{ctx}{ledgr strategy context.}

\item{lookback}{Positive integer return lookback.}
}
\value{
A \code{ledgr_signal} object.
}
\description{
\code{signal_return()} reads the \code{return_<lookback>} feature for every instrument
in \code{ctx$universe} and returns a \code{ledgr_signal}. The required indicator must
already be registered on the experiment, for example with
\code{features = list(ledgr_ind_returns(20))}; this helper never auto-registers
indicators.
}

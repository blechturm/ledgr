% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability-inspection.R
\name{ledgr_facts_inspection}
\alias{ledgr_facts_inspection}
\alias{ledgr_facts_history}
\alias{ledgr_facts_resolve}
\alias{print.ledgr_facts_history}
\alias{print.ledgr_facts_resolution}
\title{Inspect point-in-time fact evidence}
\usage{
ledgr_facts_history(
  facts,
  family,
  scope_id,
  instruments = NULL,
  from = NULL,
  to = NULL
)

ledgr_facts_resolve(facts, family, scope_id, at, instruments = NULL)

\method{print}{ledgr_facts_history}(x, ...)

\method{print}{ledgr_facts_resolution}(x, ...)
}
\arguments{
\item{facts}{A fact-family object, a \code{ledgr_facts} bundle, or a sealed
\code{ledgr_snapshot}.}

\item{family}{Either \code{"membership"} or \code{"sessions"}.}

\item{scope_id}{The membership universe or session venue identifier.}

\item{instruments}{Optional instrument identifiers. Valid only for
membership. History filters assertions; resolution retains each distinct
requested identifier, including false and unknown states.}

\item{from, to}{Optional history bounds. Membership bounds are full UTC
instants interpreted as \verb{[from, to)}. Session bounds are inclusive civil
dates.}

\item{at}{One full UTC cutoff for resolution. Bare dates are rejected.}

\item{x}{A \code{ledgr_facts_history} or \code{ledgr_facts_resolution} result.}

\item{...}{Unused.}
}
\value{
A classed list with eager tibble \code{rows} and \code{evidence} members plus
serializable \code{metadata}.
}
\description{
\code{ledgr_facts_history()} returns supplied assertions retrospectively.
\code{ledgr_facts_resolve()} returns only the state supported at one decision
cutoff. Both functions accept an in-memory fact family or bundle, or a
sealed snapshot. Results are eager and read-only.
}
\section{Information boundary}{

History may include future-effective, future-known, and audit-only evidence.
Resolution excludes evidence not yet effective and knowable at \code{at}.
Neither result is a strategy context or a selection surface.
}

\section{Errors}{

Invalid argument combinations raise \code{ledgr_facts_inspection_invalid_args}.
Missing scopes raise \code{ledgr_facts_scope_not_found}. Snapshot inputs must be
sealed and hash-valid on every call.
}

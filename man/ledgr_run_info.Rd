% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-store.R
\name{ledgr_run_info}
\alias{ledgr_run_info}
\title{Inspect one run in a ledgr experiment store}
\usage{
ledgr_run_info(snapshot, run_id)
}
\arguments{
\item{snapshot}{A sealed \code{ledgr_snapshot} object. Use
\code{ledgr_snapshot_load(db_path, snapshot_id)} to resume from a durable
DuckDB file in a new R session.}

\item{run_id}{Run identifier.}
}
\value{
A \code{ledgr_run_info} object. Important fields include \code{run_id},
\code{status}, \code{snapshot_id}, \code{snapshot_hash},
\code{strategy_source_hash}, \code{strategy_params_hash}, \code{config_hash},
\code{reproducibility_level}, \code{execution_mode}, \code{elapsed_sec},
\code{pulse_count}, \code{persist_features}, feature-cache counts,
\code{promotion_context} for runs created with \code{\link[=ledgr_promote]{ledgr_promote()}}, and
\code{error_msg} for failed runs.
}
\section{Articles}{

Exploratory sweeps and promotion:
\code{vignette("sweeps", package = "ledgr")}
\code{system.file("doc", "sweeps.html", package = "ledgr")}
}

\description{
Returns a structured \code{ledgr_run_info} object for a stored run. This function
reads run metadata and diagnostics only; it does not execute strategy code.
}
\examples{
bars <- subset(ledgr_demo_bars, instrument_id == "DEMO_01")
snapshot <- ledgr_snapshot_from_df(utils::head(bars, 10))
strategy <- function(ctx, params) ctx$flat()
exp <- ledgr_experiment(snapshot, strategy, opening = ledgr_opening(cash = 1000))
bt <- ledgr_run(exp, params = list(), run_id = "flat")
ledgr_run_info(snapshot, bt$run_id)
close(bt)
ledgr_snapshot_close(snapshot)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-dsr.R
\name{ledgr_sweep_dsr}
\alias{ledgr_sweep_dsr}
\alias{as_tibble.ledgr_sweep_dsr}
\alias{print.ledgr_sweep_dsr}
\title{Sweep-level deflated Sharpe ratio diagnostic}
\usage{
ledgr_sweep_dsr(
  sweep,
  candidates = NULL,
  effective_trials = NULL,
  distance_threshold = 0.5,
  confidence = 0.95,
  risk_free_return = 0
)

\method{as_tibble}{ledgr_sweep_dsr}(x, ...)

\method{print}{ledgr_sweep_dsr}(x, ...)
}
\arguments{
\item{sweep}{A \code{ledgr_sweep_results} object with retained completed returns.}

\item{candidates}{Optional character vector of candidate ids to include.}

\item{effective_trials}{Optional whole-number effective independent trial
count. When \code{NULL}, ledgr derives it from \code{\link[=ledgr_sweep_cluster]{ledgr_sweep_cluster()}}.}

\item{distance_threshold}{Numeric scalar in \code{[0, 2]} passed to
\code{\link[=ledgr_sweep_cluster]{ledgr_sweep_cluster()}} when \code{effective_trials} is \code{NULL}.}

\item{confidence}{Numeric scalar in \code{(0, 1)} used for the \code{significant}
status flag.}

\item{risk_free_return}{Numeric scalar per-period risk-free return to
subtract before computing Sharpe.}

\item{x}{A \code{ledgr_sweep_dsr} object.}

\item{...}{Additional arguments passed to tibble printing.}
}
\value{
A \code{ledgr_sweep_dsr} object with \code{summary} and \code{metadata}.
}
\description{
\code{ledgr_sweep_dsr()} computes a native Deflated Sharpe Ratio (DSR)
diagnostic over retained completed-candidate return panels. It is an evidence
surface only: it does not select, promote, filter, or change walk-forward
identity.
}
\details{
The diagnostic consumes \code{ledgr_sweep_returns_panel(..., value = "returns",
complete = TRUE)}. It computes per-period Sharpe, skewness, kurtosis, the
variance of candidate Sharpe ratios, and an effective independent trial count.
When \code{effective_trials} is omitted, the count comes from deterministic
retained-return clustering. The output reports a DSR probability and p-value;
it does not prove live profitability or automate promotion.
}
\examples{
\dontrun{
dsr <- ledgr_sweep_dsr(sweep)
as_tibble(dsr)
}
}
\seealso{
\code{vignette("selection-integrity", package = "ledgr")} or
\code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-inspection.R
\name{ledgr_pulse_wide}
\alias{ledgr_pulse_wide}
\title{Inspect a pulse as one wide row}
\usage{
ledgr_pulse_wide(pulse, feature_map = NULL)
}
\arguments{
\item{pulse}{A \code{ledgr_pulse_context}.}

\item{feature_map}{Optional \code{ledgr_feature_map}. Filters and orders feature
columns; does not rename them to aliases.}
}
\value{
A one-row tibble with \code{ts_utc}, \code{cash}, \code{equity}, one OHLCV
block per instrument, and feature columns for the requested pulse.
}
\description{
\code{ledgr_pulse_wide()} returns one row for a pulse. It includes pulse metadata,
portfolio state, per-instrument OHLCV columns, and per-instrument feature
columns. OHLCV columns use \verb{\{instrument_id\}__ohlcv_\{field\}}. Feature columns
use \verb{\{instrument_id\}__feature_\{feature_id\}}. The delimiter \verb{__} is reserved
for this naming contract and must not appear in instrument IDs or feature IDs
used in the wide output. A feature map filters and orders feature columns but
does not rename them to aliases.
}
\section{Articles}{

Indicators, feature IDs, and pulse feature views:

\code{vignette("indicators", package = "ledgr")}
\code{system.file("doc", "indicators.html", package = "ledgr")}
}

\examples{
\dontshow{
bars <- data.frame(
  ts_utc = ledgr_utc("2020-01-01") + 86400 * 0:2,
  instrument_id = "AAA",
  open = 100:102,
  high = 101:103,
  low = 99:101,
  close = 100:102,
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
features <- ledgr_feature_map(sma = ledgr_ind_sma(2))
pulse <- ledgr_pulse_snapshot(
  snapshot,
  universe = "AAA",
  ts_utc = ledgr_utc("2020-01-03"),
  features = features
)
}
ledgr_pulse_wide(pulse, features)
\dontshow{
close(pulse)
ledgr_snapshot_close(snapshot)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sweep-filter.R
\name{ledgr_sweep_filter}
\alias{ledgr_sweep_filter}
\title{Evaluate a sweep against a business objective}
\usage{
ledgr_sweep_filter(sweep_results, objective)
}
\arguments{
\item{sweep_results}{A \code{ledgr_sweep_results} object.}

\item{objective}{A \code{ledgr_business_objective} object.}
}
\value{
A \code{ledgr_sweep_filter_result} object with \code{tear_down} and \code{metadata}.
\code{as_tibble()} and \code{print()} expose the complete tear-down table, including
failed criteria and ineligible candidates.
}
\description{
\code{ledgr_sweep_filter()} evaluates every completed sweep candidate against
every criterion in a \code{\link[=ledgr_business_objective]{ledgr_business_objective()}}. It returns eligibility
evidence, not a selected candidate. Ineligible candidates remain in the
default table, candidate order remains sweep order, and no ranking,
promotion, persistence, or identity mutation occurs.
}
\details{
This function is not a replacement for \code{dplyr::filter()}. Its result is a
long candidate-by-criterion audit table. \code{\link[=ledgr_candidate]{ledgr_candidate()}},
\code{\link[=ledgr_promote]{ledgr_promote()}}, and walk-forward selection reject that result explicitly.
}
\examples{
\dontrun{
objective <- ledgr_business_objective(
  ledgr_objective_max_drawdown(0.20),
  ledgr_objective_min_trades(30)
)
evidence <- ledgr_sweep_filter(sweep, objective)
as_tibble(evidence)
}
}

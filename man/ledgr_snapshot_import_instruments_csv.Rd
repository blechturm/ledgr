% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots-import-instruments.R
\name{ledgr_snapshot_import_instruments_csv}
\alias{ledgr_snapshot_import_instruments_csv}
\title{Import snapshot instruments from CSV (v0.1.1)}
\usage{
ledgr_snapshot_import_instruments_csv(
  con,
  snapshot_id,
  instruments_csv_path,
  encoding = "UTF-8",
  strict = TRUE
)
}
\arguments{
\item{con}{A DBI connection to DuckDB.}

\item{snapshot_id}{Snapshot id (must exist and be status \code{CREATED}).}

\item{instruments_csv_path}{Path to instruments CSV.}

\item{encoding}{File encoding (default \code{"UTF-8"}).}

\item{strict}{If \code{TRUE}, fail loud on any contract violation.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
Imports instrument metadata into \code{snapshot_instruments} for a snapshot in
status \code{CREATED} (snapshot mutability rule).
}
\details{
CSV contract (v0.1.1 spec section 6.2):
\itemize{
\item Required columns: \code{instrument_id}, \code{symbol}
\item Optional columns (defaults): \code{currency} (\code{"USD"}), \code{asset_class} (\code{"EQUITY"}),
\code{multiplier} (\code{1.0}), \code{tick_size} (\code{0.01})
\item Encoding: UTF-8 (BOM tolerated and stripped)
}

Errors:
\itemize{
\item \code{LEDGR_SNAPSHOT_NOT_FOUND} if \code{snapshot_id} does not exist.
\item \code{LEDGR_SNAPSHOT_NOT_MUTABLE} if snapshot status is not \code{CREATED}.
\item \code{LEDGR_CSV_FORMAT_ERROR} on CSV contract/parse violations or duplicate PKs.
}
}
\examples{
db_path <- tempfile(fileext = ".duckdb")
con <- ledgr_db_init(db_path)
snapshot_id <- ledgr_snapshot_create(
  con,
  snapshot_id = "snapshot_20200101_000000_abcd"
)
instruments_csv <- tempfile(fileext = ".csv")
utils::write.csv(data.frame(
  instrument_id = "AAA",
  symbol = "AAA"
), instruments_csv, row.names = FALSE)
ledgr_snapshot_import_instruments_csv(con, snapshot_id, instruments_csv)
DBI::dbDisconnect(con, shutdown = TRUE)
}

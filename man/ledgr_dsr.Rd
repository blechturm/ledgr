% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-dsr.R
\name{ledgr_dsr}
\alias{ledgr_dsr}
\title{Deflated Sharpe ratio diagnostic}
\usage{
ledgr_dsr(
  x,
  candidates = NULL,
  effective_trials = NULL,
  distance_threshold = 0.5,
  confidence = 0.95,
  risk_free_return = 0
)
}
\arguments{
\item{x}{A \code{ledgr_return_panel} object or a \code{ledgr_sweep_results} object with
retained completed returns.}

\item{candidates}{Optional character vector of candidate ids to include.}

\item{effective_trials}{Optional whole-number effective independent trial
count. When \code{NULL}, ledgr derives it from \code{\link[=ledgr_effective_trials]{ledgr_effective_trials()}}.}

\item{distance_threshold}{Numeric scalar in \verb{[0, 2]} passed to
\code{\link[=ledgr_effective_trials]{ledgr_effective_trials()}} when \code{effective_trials} is \code{NULL}.}

\item{confidence}{Numeric scalar in \verb{(0, 1)} used for the \code{significant}
status flag.}

\item{risk_free_return}{Numeric scalar per-period risk-free return to
subtract before computing Sharpe.}
}
\value{
A \code{ledgr_dsr} object with \code{summary} and \code{metadata}.
}
\description{
\code{ledgr_dsr()} computes a native Deflated Sharpe Ratio (DSR)
diagnostic over a return panel. It is an evidence surface only: it does not
select, promote, filter, or change walk-forward identity.
}
\examples{
\dontrun{
dsr <- ledgr_dsr(sweep)
as_tibble(dsr)
}
}
\seealso{
\code{vignette("selection-integrity", package = "ledgr")} or
\code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

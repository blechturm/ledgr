% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-pbo.R
\name{ledgr_sweep_pbo}
\alias{ledgr_sweep_pbo}
\alias{as_tibble.ledgr_sweep_pbo}
\alias{print.ledgr_sweep_pbo}
\title{Sweep-level PBO/CSCV diagnostic}
\usage{
ledgr_sweep_pbo(
  sweep,
  candidates = NULL,
  S = 4L,
  metric = NULL,
  metric_name = NULL,
  threshold = 0
)

\method{as_tibble}{ledgr_sweep_pbo}(
  x,
  what = c("summary", "cases", "degradation"),
  ...
)

\method{print}{ledgr_sweep_pbo}(x, ...)
}
\arguments{
\item{sweep}{A \code{ledgr_sweep_results} object with retained completed returns.}

\item{candidates}{Optional character vector of candidate ids to include.}

\item{S}{Even positive number of contiguous CSCV subsets. \code{S} must divide the
post-first-row return count.}

\item{metric}{Optional function that receives a numeric returns matrix and
returns one finite numeric score per candidate column. Higher scores are
treated as better. When \code{NULL}, mean period return is used.}

\item{metric_name}{Optional character scalar naming the metric in result
metadata.}

\item{threshold}{Numeric logit threshold. PBO is the fraction of CSCV cases
with \code{lambda <= threshold}.}

\item{x}{A \code{ledgr_sweep_pbo} object.}

\item{what}{Which table to return: \code{"summary"}, \code{"cases"}, or
\code{"degradation"}.}

\item{...}{Passed to the tibble print method.}
}
\value{
A \code{ledgr_sweep_pbo} object with \code{summary}, \code{cases},
\code{degradation}, and \code{metadata} tables/lists. Use \code{as_tibble(x)},
\code{as_tibble(x, what = "cases")}, or
\code{as_tibble(x, what = "degradation")} for programmatic access.
}
\description{
\code{ledgr_sweep_pbo()} computes a native Probability of Backtest Overfitting
(PBO) diagnostic using Combinatorially Symmetric Cross Validation (CSCV) over
retained completed-candidate return panels. It is an evidence surface only: it
does not select, promote, filter, or change walk-forward identity.
}
\details{
The diagnostic consumes \code{ledgr_sweep_returns_panel(..., value = "returns",
complete = TRUE)}. That means the structural first \code{NA_real_} return row is
verified and dropped before the CSCV matrix is formed, all selected candidates
must share one common timestamp grid, and failed or unretained candidates fail
closed through the retained-return panel classes.

\code{S} partitions the post-first-row return panel into contiguous subsets.
For each symmetric split, ledgr scores all candidates in sample, takes the
in-sample winner, ranks that same candidate out of sample, converts the relative
rank into \code{lambda = log(omega_bar / (1 - omega_bar))}, and reports PBO as
the share of cases with \code{lambda <= threshold}.

The default metric is mean period return. Custom metrics must return one finite
numeric score per candidate column; larger scores are interpreted as better.
}
\examples{
\dontrun{
pbo <- ledgr_sweep_pbo(sweep, S = 4)
as_tibble(pbo)
as_tibble(pbo, what = "cases")
}
}
\seealso{
\code{vignette("selection-integrity", package = "ledgr")} or
\code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/business-objective.R
\name{ledgr_objective_criteria}
\alias{ledgr_objective_criteria}
\alias{ledgr_objective_even_trades}
\alias{ledgr_objective_even_profit}
\alias{ledgr_objective_stable_region}
\alias{ledgr_objective_max_drawdown}
\alias{ledgr_objective_stable_runs}
\alias{ledgr_objective_min_trades}
\alias{ledgr_objective_positive_trajectory}
\alias{ledgr_objective_diagnostic_threshold}
\title{Business-objective criteria}
\usage{
ledgr_objective_even_trades(max_concentration = 0.3)

ledgr_objective_even_profit(max_concentration = 0.4)

ledgr_objective_stable_region(
  min_neighbors = 2L,
  metric = "sharpe_ratio",
  direction = c("maximize", "minimize"),
  parameter_columns = NULL
)

ledgr_objective_max_drawdown(max_drawdown = 0.2)

ledgr_objective_stable_runs(max_streak = 10L)

ledgr_objective_min_trades(n = 30L)

ledgr_objective_positive_trajectory(slope_min = 0)

ledgr_objective_diagnostic_threshold(x, column, threshold, comparison = NULL)
}
\arguments{
\item{max_concentration}{Finite scalar in \verb{(0, 1]} giving the largest
acceptable concentration share.}

\item{min_neighbors}{Positive whole-number minimum count of supporting
Manhattan-distance-1 lattice neighbors.}

\item{metric}{Non-empty sweep-summary metric column used by the strict
lattice detector.}

\item{direction}{Either \code{"maximize"} or \code{"minimize"}; values are normalized
so larger detector scores are better.}

\item{parameter_columns}{Optional non-empty character vector naming the
candidate parameter axes. \code{NULL} uses all resolved varying parameter
columns supplied to the detector.}

\item{max_drawdown}{Finite scalar in \verb{[0, 1]} giving the largest acceptable
retained maximum drawdown.}

\item{max_streak}{Positive whole number giving the largest acceptable
consecutive winning or losing run.}

\item{n}{Non-negative whole-number minimum closed-trade count.}

\item{slope_min}{Finite minimum slope in log-equity units per retained
observation.}

\item{x}{A \code{ledgr_dsr} or \code{ledgr_min_track_record} result.}

\item{column}{An admissible candidate-level diagnostic output column.}

\item{threshold}{A finite numeric threshold for numeric columns, or one
required status string for a \code{status} column.}

\item{comparison}{Optional comparison. Numeric defaults are \code{"at_least"}
for \code{dsr_probability} and \code{"at_most"} for
\code{min_track_record_length}; status columns require equality.}
}
\value{
A classed internal-contract \code{ledgr_objective_criterion} object.
}
\description{
These constructors create ledgr-owned, serializable criterion steps for
\code{\link[=ledgr_business_objective]{ledgr_business_objective()}}. Each step thresholds already-computed evidence
and returns eligibility evidence only. Steps do not rank, select, promote,
persist, or alter execution or walk-forward identity.
}
\details{
\code{ledgr_objective_even_trades()} divides the sweep scoring interval into four
equal-duration bins and measures the largest bin's share of closed trades.
\code{ledgr_objective_even_profit()} measures the largest single trade's share of
total absolute closed-trade realized P&L. \code{ledgr_objective_stable_runs()}
measures the longest consecutive WIN or LOSS run in \code{trade_seq} order;
BREAKEVEN rows reset a run.

\code{ledgr_objective_stable_region()} implements ledgr's strict-lattice
operationalization of Pardo's broad-plateau idea. It is not a transcription
of a Pardo formula and does not support sparse or unordered parameter spaces.
Lattice-boundary candidates have fewer available neighbors and cannot pass
when \code{min_neighbors} exceeds that available count; the detector retains the
available-neighbor count as audit evidence.

\code{ledgr_objective_diagnostic_threshold()} embeds a hashed snapshot of one
already-computed \code{\link[=ledgr_dsr]{ledgr_dsr()}} or \code{\link[=ledgr_min_track_record]{ledgr_min_track_record()}} result. It never
recomputes the diagnostic. Admissible columns are \code{dsr_probability} and
\code{status} for DSR, and \code{min_track_record_length} and \code{status} for MinTRL.
Numeric diagnostic thresholds treat \code{Inf} and \code{-Inf} as determinate boundary
evidence; missing values and \code{NaN} fail closed.
}
\examples{
ledgr_objective_even_trades(0.30)
ledgr_objective_even_profit(0.40)
ledgr_objective_stable_region(min_neighbors = 2)
ledgr_objective_max_drawdown(0.20)
ledgr_objective_stable_runs(10)
ledgr_objective_min_trades(30)
ledgr_objective_positive_trajectory(0)
}
\references{
Pardo, R. (2008). \emph{The Evaluation and Optimization of Trading
Strategies}, Chapter 11. Stable-region adjacency and tolerance are ledgr's
documented operationalization, not a Pardo-authored formula.
}

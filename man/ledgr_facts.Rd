% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability-facts.R
\name{ledgr_facts}
\alias{ledgr_facts}
\alias{ledgr_facts_membership_intervals}
\alias{ledgr_facts_membership_snapshots}
\alias{ledgr_facts_trading_status}
\alias{ledgr_facts_lifetime}
\alias{ledgr_facts_sessions}
\title{Point-in-time availability facts}
\usage{
ledgr_facts_membership_intervals(
  df,
  universe_id,
  knowledge = c("evidenced", "assume_effective")
)

ledgr_facts_membership_snapshots(
  df,
  universe_id,
  complete = TRUE,
  knowledge = c("evidenced", "assume_effective")
)

ledgr_facts_trading_status(df, knowledge = c("evidenced", "assume_effective"))

ledgr_facts_lifetime(df, knowledge = c("evidenced", "assume_effective"))

ledgr_facts_sessions(
  df,
  venue_id,
  knowledge = c("evidenced", "assume_effective"),
  timezone = "UTC"
)

ledgr_facts(...)
}
\arguments{
\item{df}{A data frame containing one fact row per assertion. See Details.}

\item{universe_id}{A non-empty identifier for the membership universe.}

\item{knowledge}{Either \code{"evidenced"} or \code{"assume_effective"}.}

\item{complete}{Logical scalar or one value per input row. Complete
membership snapshots establish omitted instruments as non-members;
partial snapshots leave omission unknown. Values must agree within each
dated snapshot group.}

\item{venue_id}{A non-empty venue identifier.}

\item{timezone}{An IANA timezone used to interpret session dates and times.}

\item{...}{Fact-family objects returned by these constructors.}
}
\value{
A classed fact-family object, or a \code{ledgr_facts} bundle.
}
\description{
These constructors normalize point-in-time membership, trading-status,
session-calendar, and lifetime evidence before snapshot creation. Effective
intervals are half-open: \code{effective_from} is included and \code{effective_to} is
excluded. Missing knowledge time remains audit-only unless
\code{knowledge = "assume_effective"} is declared.
}
\details{
Membership intervals require \code{instrument_id}, \code{effective_from}, and
\code{member}; \code{effective_to} and \code{knowledge_time} are optional. Membership
snapshots require \code{effective_from} and \code{instrument_id}; an \code{NA}
\code{instrument_id} records an empty set header. Trading-status rows require
\code{instrument_id}, \code{effective_from}, \code{status}, and \code{source}. Status is one of
\code{active}, \code{halted}, or \code{quotation_only}; optional columns are
\code{effective_to}, \code{knowledge_time}, \code{precedence}, \code{fact_id}, \code{revision_id},
and \code{supersedes_fact_id}. Lifetime rows require \code{instrument_id},
\code{effective_from}, and \code{assertion}, where assertion is \code{known_active},
\code{known_inactive}, or \code{unknown}.

Session rows require \code{session_date}, \code{status}, \code{session_open}, and
\code{session_close}; \code{knowledge_time} is required under evidenced knowledge and
may be omitted only with \code{knowledge = "assume_effective"}. Every civil date
in the declared range appears exactly once. Open-session times may be
POSIXct timestamps or local \code{HH:MM:SS} strings; closed rows have missing open
and close times.
}
\section{Errors}{

Structural input failures inherit from \code{ledgr_invalid_args}. The primary
classes are \code{ledgr_fact_invalid_shape}, \code{ledgr_fact_invalid_interval},
\code{ledgr_fact_identity_conflict}, \code{ledgr_fact_structural_conflict},
\code{ledgr_fact_invalid_supersession}, and \code{ledgr_session_invalid}.
}

\examples{
membership <- ledgr_facts_membership_snapshots(
  data.frame(
    effective_from = as.POSIXct("2020-01-02", tz = "UTC"),
    knowledge_time = as.POSIXct("2020-01-01", tz = "UTC"),
    instrument_id = "AAA"
  ),
  universe_id = "example"
)
facts <- ledgr_facts(membership)
facts
}

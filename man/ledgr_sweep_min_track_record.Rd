% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-min-track-record.R
\name{ledgr_sweep_min_track_record}
\alias{ledgr_sweep_min_track_record}
\alias{as_tibble.ledgr_sweep_min_track_record}
\alias{print.ledgr_sweep_min_track_record}
\title{Sweep-level minimum track record length diagnostic}
\usage{
ledgr_sweep_min_track_record(
  sweep,
  candidates = NULL,
  reference_sharpe = 0,
  confidence = 0.95,
  risk_free_return = 0
)

\method{as_tibble}{ledgr_sweep_min_track_record}(x, ...)

\method{print}{ledgr_sweep_min_track_record}(x, ...)
}
\arguments{
\item{sweep}{A \code{ledgr_sweep_results} object with retained completed returns.}

\item{candidates}{Optional character vector of candidate ids to include.}

\item{reference_sharpe}{Numeric scalar reference Sharpe ratio in the same
per-period units as the retained return series. The default asks whether
the observed Sharpe is significantly above zero.}

\item{confidence}{Numeric scalar confidence level in \code{(0, 1)}.}

\item{risk_free_return}{Numeric scalar per-period risk-free return to
subtract before computing Sharpe. The default is zero.}

\item{x}{A \code{ledgr_sweep_min_track_record} object.}

\item{...}{Additional arguments passed to tibble printing.}
}
\value{
A \code{ledgr_sweep_min_track_record} object with \code{summary} and
\code{metadata}. Use \code{as_tibble(x)} for programmatic access.
}
\description{
\code{ledgr_sweep_min_track_record()} computes the minimum track record length
(MinTRL) diagnostic over retained completed-candidate return panels. It is an
evidence surface only: it does not select, promote, filter, or change
walk-forward identity.
}
\section{Articles}{

\code{vignette("selection-integrity", package = "ledgr")}
\code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

\examples{
\dontrun{
min_trl <- ledgr_sweep_min_track_record(sweep, reference_sharpe = 0)
as_tibble(min_trl)
}
}
\seealso{
\code{vignette("selection-integrity", package = "ledgr")} or
  \code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

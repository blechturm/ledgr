% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots-import-bars.R
\name{ledgr_snapshot_import_bars_csv}
\alias{ledgr_snapshot_import_bars_csv}
\title{Import snapshot bars from CSV}
\usage{
ledgr_snapshot_import_bars_csv(
  con,
  snapshot_id,
  bars_csv_path,
  instruments_csv_path = NULL,
  auto_generate_instruments = TRUE,
  encoding = "UTF-8",
  validate = c("fail_fast", "none")
)
}
\arguments{
\item{con}{A DBI connection to DuckDB.}

\item{snapshot_id}{Snapshot id (must exist and be status \code{CREATED}).}

\item{bars_csv_path}{Path to bars CSV.}

\item{instruments_csv_path}{Optional path to instruments CSV.}

\item{auto_generate_instruments}{If TRUE and \code{instruments_csv_path} is NULL,
auto-generate instruments from bars.}

\item{encoding}{File encoding (default \code{"UTF-8"}).}

\item{validate}{Validation mode (default \code{"fail_fast"}).}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
Imports EOD bars into \code{snapshot_bars} for a snapshot in status \code{CREATED}
(snapshot mutability rule). Optionally imports instruments from CSV, or
auto-generates them from bars.
}
\details{
CSV contract:
\itemize{
\item Required columns: \code{instrument_id}, \code{ts_utc}, \code{open}, \code{high}, \code{low}, \code{close}
\item Optional columns: \code{volume} (defaults to \code{NA})
\item Timestamp format: ISO8601 UTC with trailing \code{Z}, e.g. \verb{2020-01-01T00:00:00Z}
\item Encoding: UTF-8 (BOM tolerated and stripped)
\item Rounding: OHLCV are rounded to 8 decimals on import
\item Extra columns: ignored; only canonical bar columns are persisted and hashed
}

Errors:
\itemize{
\item \code{LEDGR_SNAPSHOT_NOT_FOUND} if \code{snapshot_id} does not exist.
\item \code{LEDGR_SNAPSHOT_NOT_MUTABLE} if snapshot status is not \code{CREATED}.
\item \code{LEDGR_CSV_FORMAT_ERROR} on CSV contract/parse violations or duplicate PKs.
}

CSV and OHLC errors are import/seal errors. They occur before a snapshot can
be loaded into an experiment or executed by a strategy.
}
\section{Low-level CSV lifecycle}{

Most users should prefer \code{ledgr_snapshot_from_csv()}. Use
\code{ledgr_snapshot_import_bars_csv()} directly only when you need to control the
create/import/seal lifecycle yourself:
\enumerate{
\item create the snapshot envelope with \code{ledgr_snapshot_create()};
\item import bars into that \code{CREATED} snapshot;
\item seal it with \code{ledgr_snapshot_seal()} to validate bars and write the
snapshot hash;
\item inspect metadata with \code{ledgr_snapshot_info()} if needed;
\item disconnect the write connection and reopen with
\code{ledgr_snapshot_open(..., verify = TRUE)};
\item pass the loaded snapshot to \code{ledgr_experiment()} and \code{ledgr_run()}.
}

\code{bar_count} and \code{instrument_count} are live counts from the sealed snapshot
tables. The raw \code{meta_json} is envelope metadata on the snapshot row;
seal-time metadata inside it uses \code{n_bars} and \code{n_instruments}. Snapshot
identity does not come from that metadata. \code{snapshot_hash} identifies the
normalized bars and instruments only.
}

\section{Articles}{

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}
}

\examples{
db_path <- tempfile(fileext = ".duckdb")
con <- ledgr_db_init(db_path)
snapshot_id <- ledgr_snapshot_create(
  con,
  snapshot_id = "snapshot_20200101_000000_abcd"
)
bars_csv <- tempfile(fileext = ".csv")
utils::write.csv(data.frame(
  instrument_id = "AAA",
  ts_utc = c("2020-01-01T00:00:00Z", "2020-01-02T00:00:00Z"),
  open = c(100, 101),
  high = c(101, 102),
  low = c(99, 100),
  close = c(100, 101),
  volume = 1000
), bars_csv, row.names = FALSE)
ledgr_snapshot_import_bars_csv(con, snapshot_id, bars_csv)
snapshot_hash <- ledgr_snapshot_seal(con, snapshot_id)
snapshot_info <- ledgr_snapshot_info(con, snapshot_id)
DBI::dbDisconnect(con, shutdown = TRUE)

snapshot <- ledgr_snapshot_open(
  db_path,
  snapshot_id = snapshot_id,
  verify = TRUE
)
snapshot_hash
snapshot_info[
  ,
  c("snapshot_id", "status", "snapshot_hash", "bar_count", "instrument_count")
]
ledgr_snapshot_close(snapshot)
}

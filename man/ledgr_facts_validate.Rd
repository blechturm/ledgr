% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability-facts.R
\name{ledgr_facts_validate}
\alias{ledgr_facts_validate}
\title{Validate point-in-time facts without writing}
\usage{
ledgr_facts_validate(
  facts,
  bars_df,
  instruments_df = NULL,
  invalid_observations = c("error", "quarantine")
)
}
\arguments{
\item{facts}{A \code{ledgr_facts} bundle.}

\item{bars_df}{Bars intended for \code{\link[=ledgr_snapshot_from_df]{ledgr_snapshot_from_df()}}.}

\item{instruments_df}{Optional instrument master intended for the snapshot.}

\item{invalid_observations}{Either \code{"error"} or \code{"quarantine"}.}
}
\value{
A \code{ledgr_facts_report} object with \code{summary}, \code{facts}, and
\code{observations} tibbles plus a logical \code{can_seal} field.
}
\description{
Produces a source-aware dry-run report. Accepted rows, retained runtime
conflicts, rejected rows, quarantine candidates, and audit-only facts stay
distinct. This function never creates or modifies a snapshot.
}
\section{Errors}{

Invalid or mutated bundles raise \code{ledgr_invalid_facts}. Snapshot creation
raises \code{ledgr_availability_validation_failed} with this report attached when
accepted input cannot be sealed.
}

\examples{
sessions <- data.frame(
  session_date = as.Date("2020-01-01") + 0:1,
  status = c("closed", "open"),
  session_open = c(NA, "09:30:00"),
  session_close = c(NA, "16:00:00"),
  knowledge_time = as.POSIXct("2019-12-31", tz = "UTC")
)
facts <- ledgr_facts(ledgr_facts_sessions(sessions, "example", timezone = "UTC"))
bars <- data.frame(
  instrument_id = "AAA",
  ts_utc = as.Date("2020-01-02"),
  open = 10, high = 11, low = 9, close = 10
)
ledgr_facts_validate(facts, bars)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots-create.R
\name{ledgr_snapshot_create}
\alias{ledgr_snapshot_create}
\title{Create a snapshot (v0.1.1)}
\usage{
ledgr_snapshot_create(con, snapshot_id = NULL, meta = NULL)
}
\arguments{
\item{con}{A DBI connection to DuckDB.}

\item{snapshot_id}{Optional snapshot identifier.}

\item{meta}{Optional JSON-safe metadata list (stored as canonical JSON).}
}
\value{
The snapshot_id (character(1)).
}
\description{
Creates a row in \code{snapshots} with status \code{CREATED}. Does not import any data.
}
\details{
Snapshot mutability rule: snapshots are mutable only while their status is
\code{CREATED} (imports/seal will fail for \code{SEALED} and \code{FAILED} snapshots).

If \code{snapshot_id} is \code{NULL}, generates an id of the form
\verb{snapshot_\{YYYYmmdd_HHMMSS\}_\{4-hex\}} (v0.1.1 spec, appendix A section 3.1).

Timestamps are stored in UTC and normalized to ISO8601 \code{...Z} form.

Errors:
\itemize{
\item \code{ledgr_snapshot_exists} if \code{snapshot_id} already exists.
}
}
\section{Articles}{

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}
}

\examples{
db_path <- tempfile(fileext = ".duckdb")
con <- ledgr_db_init(db_path)
snapshot_id <- ledgr_snapshot_create(
  con,
  snapshot_id = "snapshot_20200101_000000_abcd"
)
snapshot_id
DBI::dbDisconnect(con, shutdown = TRUE)
}

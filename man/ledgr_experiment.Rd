% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment.R
\name{ledgr_experiment}
\alias{ledgr_experiment}
\title{Define a reusable ledgr experiment}
\usage{
ledgr_experiment(
  snapshot,
  strategy,
  features = list(),
  opening = ledgr_opening(cash = 1e+05),
  universe = NULL,
  timing_model = ledgr_timing_next_open(),
  cost_model,
  risk_chain = ledgr_risk_none(),
  fill_model = NULL,
  persist_features = TRUE,
  execution_mode = "audit_log",
  metric_context = NULL,
  risk_free_rate = NULL
)
}
\arguments{
\item{snapshot}{A sealed \code{ledgr_snapshot}.}

\item{strategy}{A function with signature \verb{function(ctx, params)}.}

\item{features}{List of \code{ledgr_indicator}/\code{ledgr_indicator_bundle} objects, a
single indicator or bundle, or a \code{ledgr_feature_map}. Compatibility note:
\verb{function(params)} feature factories remain supported for old flat-grid and
advanced exact-ID workflows, but new parameterized feature sweeps should use
\code{ledgr_feature_map()} with \code{ledgr_param()}, \code{ledgr_feature_grid()},
\code{ledgr_strategy_grid()}, and \code{ledgr_grid_cross()}.}

\item{opening}{A \code{ledgr_opening} object.}

\item{universe}{Character vector of instrument IDs, or \code{NULL} for all
instruments in the snapshot.}

\item{timing_model}{Timing model object. Defaults to
\code{ledgr_timing_next_open()}. The timing model proposes fills; cost models
resolve fill prices and explicit fees.}

\item{cost_model}{Required ledgr cost model object. Use \code{ledgr_cost_zero()}
for explicit zero-cost execution. \code{ledgr_cost_spread_bps()} treats
\code{spread_bps} as a quoted bid/ask spread: buys cross half the spread above
the reference price and sells cross half the spread below it, so a round trip
crosses approximately \code{spread_bps} basis points before explicit fees.}

\item{risk_chain}{Target-risk chain object. Defaults to
\code{ledgr_risk_none()} for explicit no-risk execution.}

\item{fill_model}{Legacy v0.1.8 fill model argument. Supplying it now fails
with \code{ledgr_legacy_fill_model_shape}; use \code{timing_model} plus
\code{cost_model}.}

\item{persist_features}{Logical scalar.}

\item{execution_mode}{Execution mode (\code{"audit_log"} or \code{"db_live"}).}

\item{metric_context}{Optional \code{ledgr_metric_context} object or scalar
annual risk-free rate shorthand. \code{NULL} uses the default metric context.}

\item{risk_free_rate}{Optional shorthand for the annual risk-free rate.
Supply either \code{metric_context} or \code{risk_free_rate}, not both.}
}
\value{
A \code{ledgr_experiment} object.
}
\description{
\code{ledgr_experiment()} bundles a sealed snapshot, strategy, features, opening
state, and execution options into the experiment-first public object used by
the v0.1.7 workflow. The constructor validates shape only; it does not run a
strategy or write run artifacts.
}
\section{Articles}{

Strategy authoring:
\code{vignette("strategy-development", package = "ledgr")}
\code{system.file("doc", "strategy-development.html", package = "ledgr")}

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}

Reproducibility model:
\code{vignette("reproducibility", package = "ledgr")}
\code{system.file("doc", "reproducibility.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
strategy <- function(ctx, params) {
  stats::setNames(rep(0, length(ctx$universe)), ctx$universe)
}
exp <- ledgr_experiment(snapshot, strategy, cost_model = ledgr_cost_zero())
inherits(exp, "ledgr_experiment")
ledgr_snapshot_close(snapshot)
}

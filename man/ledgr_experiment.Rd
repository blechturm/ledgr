% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment.R
\name{ledgr_experiment}
\alias{ledgr_experiment}
\title{Define a reusable ledgr experiment}
\usage{
ledgr_experiment(
  snapshot,
  strategy,
  features = list(),
  opening = ledgr_opening(cash = 1e+05),
  universe = NULL,
  fill_model = NULL,
  persist_features = TRUE,
  execution_mode = "audit_log"
)
}
\arguments{
\item{snapshot}{A sealed \code{ledgr_snapshot}.}

\item{strategy}{A function with signature \verb{function(ctx, params)}.}

\item{features}{List of \code{ledgr_indicator} objects, a \code{ledgr_feature_map}, or
a function with signature \verb{function(params)} returning one of those forms
at run time.}

\item{opening}{A \code{ledgr_opening} object.}

\item{universe}{Character vector of instrument IDs, or \code{NULL} for all
instruments in the snapshot.}

\item{fill_model}{Fill model config. \code{NULL} uses ledgr's default next-open
model with zero spread and zero fixed commission.}

\item{persist_features}{Logical scalar.}

\item{execution_mode}{Execution mode (\code{"audit_log"} or \code{"db_live"}).}
}
\value{
A \code{ledgr_experiment} object.
}
\description{
\code{ledgr_experiment()} bundles a sealed snapshot, strategy, features, opening
state, and execution options into the experiment-first public object used by
the v0.1.7 workflow. The constructor validates shape only; it does not run a
strategy or write run artifacts.
}
\section{Articles}{

Strategy authoring:
\code{vignette("strategy-development", package = "ledgr")}
\code{system.file("doc", "strategy-development.html", package = "ledgr")}

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}

Reproducibility model:
\code{vignette("reproducibility", package = "ledgr")}
\code{system.file("doc", "reproducibility.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
strategy <- function(ctx, params) {
  stats::setNames(rep(0, length(ctx$universe)), ctx$universe)
}
exp <- ledgr_experiment(snapshot, strategy)
inherits(exp, "ledgr_experiment")
ledgr_snapshot_close(snapshot)
}

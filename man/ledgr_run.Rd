% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{ledgr_run}
\alias{ledgr_run}
\title{Run a ledgr experiment}
\usage{
ledgr_run(
  exp,
  params = list(),
  feature_params = list(),
  run_id = NULL,
  seed = NULL,
  compiled_accounting_model = NULL
)
}
\arguments{
\item{exp}{A \code{ledgr_experiment} object.}

\item{params}{JSON-safe list passed to \verb{function(ctx, params)} strategy.}

\item{feature_params}{JSON-safe list used to resolve parameterized feature
declarations before execution.}

\item{run_id}{Optional run identifier.}

\item{seed}{Optional integer-like execution seed. When non-\code{NULL}, ledgr
applies it at fold entry and stores it in run identity.}

\item{compiled_accounting_model}{Optional accounting accelerator selector.
\code{NULL} uses the canonical R accounting path. \code{"spot_fifo"} is a scoped
spot-asset FIFO accelerator for memory-backed sweep execution; committed
durable runs currently fail closed because durable compiled integration is
deferred.}
}
\value{
A \code{ledgr_backtest} object.
}
\description{
\code{ledgr_run()} is the public single-run API for the v0.1.7
experiment-first workflow. It evaluates run-time feature definitions,
builds the canonical backtest config, and delegates to the shared runner.
}
\section{Identity}{

Run identity fields, including \code{config_hash}, \code{feature_set_hash},
\code{cost_model_hash}, and \code{cost_plan_json}, are summarized in
\link{ledgr_identity_fields}.
}

\section{Articles}{

Strategy authoring:
\code{vignette("strategy-development", package = "ledgr")}
\code{system.file("doc", "strategy-development.html", package = "ledgr")}

Metrics and accounting:
\code{vignette("metrics-and-accounting", package = "ledgr")}
\code{system.file("doc", "metrics-and-accounting.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- params$qty
  targets
}
exp <- ledgr_experiment(snapshot, strategy, cost_model = ledgr_cost_zero())
bt <- ledgr_run(exp, params = list(qty = 1), run_id = "example-run")
close(bt)
ledgr_snapshot_close(snapshot)
}

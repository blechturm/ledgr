% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{close.ledgr_backtest}
\alias{close.ledgr_backtest}
\title{Close a backtest result connection}
\usage{
\method{close}{ledgr_backtest}(con, ...)
}
\arguments{
\item{con}{A \code{ledgr_backtest} object.}

\item{...}{Unused.}
}
\value{
The input object, invisibly.
}
\description{
Releases any open DuckDB connection held by a \code{ledgr_backtest} object. The
underlying DuckDB file is not deleted.
}
\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- 1
  targets
}
bt <- ledgr_backtest(data = bars, strategy = strategy, initial_cash = 1000)
close(bt)
}

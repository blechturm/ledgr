% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{summary.ledgr_backtest}
\alias{summary.ledgr_backtest}
\title{Summarize a backtest result}
\usage{
\method{summary}{ledgr_backtest}(
  object,
  metrics = "standard",
  metric_context = NULL,
  risk_free_rate = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{ledgr_backtest} object.}

\item{metrics}{Only \code{"standard"} is supported.}

\item{metric_context}{Optional metric context override for this summary.
When omitted, ledgr uses the metric context stored with the run.}

\item{risk_free_rate}{Optional scalar annual risk-free-rate override as a
decimal. Supply either \code{metric_context} or \code{risk_free_rate}, not both.}

\item{...}{Unused.}
}
\value{
The input \code{ledgr_backtest} object, invisibly. The printed values are
descriptive output; use \code{ledgr_compute_metrics()} for a named list of the
same metric values.
}
\description{
Prints standard performance, risk, trade, and exposure metrics.
}
\details{
The standard summary displays:
\itemize{
\item total return: last public equity row divided by the first public equity
row minus 1;
\item annualized return: geometric annualized return from the first and last
public equity rows using the metric context's annualization calendar;
\item max drawdown: maximum peak-to-trough decline,
\code{min(equity / cummax(equity) - 1)};
\item annualized volatility: standard deviation of adjacent equity-row returns
multiplied by \code{sqrt(bars_per_year)};
\item Sharpe ratio: annualized ratio of average period excess return to
excess-return standard deviation, using the metric context's scalar annual
risk-free rate converted to a per-period return;
\item total trades: number of closed trade rows, not number of fill rows;
\item win rate: share of closed trade rows with strict \code{realized_pnl > 0};
\item average trade: mean \code{realized_pnl} across closed trade rows;
\item time in market: share of equity rows with absolute
\code{positions_value > 1e-6}.
}

If there are no closed trade rows, total trades is zero and win rate and
average trade are printed as not available. If registered features cannot
become usable because an instrument has fewer bars than the feature contract
requires, the summary prints a compact Warmup Diagnostics section naming the
feature ID, instrument ID, required bars, and available bars.
}
\section{Articles}{

Metrics and accounting:
\code{vignette("metrics-and-accounting", package = "ledgr")}
\code{system.file("doc", "metrics-and-accounting.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:3,
  instrument_id = "AAA",
  open = c(100, 101, 102, 103),
  high = c(101, 102, 103, 104),
  low = c(99, 100, 101, 102),
  close = c(100, 101, 102, 103),
  volume = 1000
)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- 1
  targets
}
bt <- ledgr_backtest(data = bars, strategy = strategy, initial_cash = 1000, cost_model = ledgr_cost_zero())
summary(bt)
close(bt)
}

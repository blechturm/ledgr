% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator-ttr.R
\name{ledgr_ind_ttr_outputs}
\alias{ledgr_ind_ttr_outputs}
\title{Construct a bundle from all or selected outputs of a TTR indicator}
\usage{
ledgr_ind_ttr_outputs(
  ttr_fn,
  input,
  ...,
  outputs = NULL,
  prefix = ledgr_ttr_id_token(ttr_fn),
  naming = NULL,
  requires_bars = NULL,
  stable_after = requires_bars
)
}
\arguments{
\item{ttr_fn}{TTR function name, for example \code{"BBands"} or \code{"MACD"}.}

\item{input}{ledgr input shape. Supported values are \code{"close"}, \code{"hl"},
\code{"hlc"}, \code{"ohlc"}, and \code{"hlcv"}.}

\item{...}{Explicit arguments forwarded to the TTR function.}

\item{outputs}{Optional character vector of backend output columns to keep.
\code{NULL} keeps all discovered outputs. For \code{MACD}, ledgr also exposes a
derived \code{histogram} output.}

\item{prefix}{Feature-ID prefix. The default derives a normalized prefix from
\code{ttr_fn}, for example \code{"BBands"} becomes \code{bbands_}. Use a character scalar
such as \code{"bb"} for concise IDs, or \code{NULL} to opt into raw output names.}

\item{naming}{Optional advanced rename escape hatch. Supply a named character
vector whose names are backend outputs and whose values are feature IDs.}

\item{requires_bars}{Explicit warmup length. Required for unknown TTR
functions. For known functions this is inferred from explicit arguments.}

\item{stable_after}{First stable row. Defaults to \code{requires_bars}.}
}
\value{
A \code{ledgr_indicator_bundle} object.
}
\description{
\code{ledgr_ind_ttr_outputs()} is an authoring convenience for multi-column TTR
functions such as \code{BBands}, \code{MACD}, \code{ATR}, \code{aroon}, and
\code{DonchianChannel}. It returns a \code{ledgr_indicator_bundle} containing ordinary
single-output \code{ledgr_indicator} objects. The bundle is flattened before
runtime feature computation, so strategies still read scalar feature IDs.
}
\section{Articles}{

Indicators, feature IDs, and warmup:
\code{vignette("indicators", package = "ledgr")}
\code{system.file("doc", "indicators.html", package = "ledgr")}
}

\examples{
if (requireNamespace("TTR", quietly = TRUE)) {
  bb <- ledgr_ind_ttr_outputs("BBands", input = "close", n = 20)
  ledgr_feature_id(bb)

  bb_short <- ledgr_ind_ttr_outputs(
    "BBands",
    input = "close",
    outputs = c("dn", "up"),
    prefix = "bb",
    n = 20
  )
  ledgr_feature_id(bb_short)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{ledgr_extract_fills}
\alias{ledgr_extract_fills}
\title{Extract fill events from a backtest}
\usage{
ledgr_extract_fills(bt, lazy = FALSE, stream_threshold = 100000L)
}
\arguments{
\item{bt}{A \code{ledgr_backtest} object.}

\item{lazy}{If \code{TRUE}, return a streaming cursor instead of materializing all rows.}

\item{stream_threshold}{Number of fill rows above which lazy mode is forced.}
}
\value{
A tibble of fill rows, or a \code{ledgr_fills_cursor} when \code{lazy = TRUE}.
}
\description{
Extract fill events from a backtest
}
\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:3,
  instrument_id = "AAA",
  open = c(100, 101, 102, 103),
  high = c(101, 102, 103, 104),
  low = c(99, 100, 101, 102),
  close = c(100, 101, 102, 103),
  volume = 1000
)
strategy <- function(ctx) {
  targets <- ctx$targets()
  targets["AAA"] <- 1
  targets
}
bt <- ledgr_backtest(data = bars, strategy = strategy, initial_cash = 1000)
ledgr_extract_fills(bt)
close(bt)
}

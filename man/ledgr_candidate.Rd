% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sweep.R, R/walk-forward-inspection.R
\name{ledgr_candidate}
\alias{ledgr_candidate}
\alias{ledgr_candidate.default}
\alias{ledgr_candidate.ledgr_walk_forward_results}
\title{Select one sweep candidate for promotion}
\usage{
ledgr_candidate(results, ...)

\method{ledgr_candidate}{default}(results, which = 1L, allow_failed = FALSE, ...)

\method{ledgr_candidate}{ledgr_walk_forward_results}(
  results,
  fold_seq,
  selection_rationale = NULL,
  snapshot = NULL,
  ...
)
}
\arguments{
\item{results}{A \code{ledgr_sweep_results} object, a tibble-like object with
\code{candidate_id}, \code{params}, \code{execution_seed}, and \code{provenance} columns, or a
\code{ledgr_walk_forward_results} object.}

\item{...}{Method-specific arguments.}

\item{which}{Candidate selector. A character scalar selects by
\code{candidate_id}; an integer-like scalar selects by row position.}

\item{allow_failed}{Logical. Failed candidates error by default.}

\item{fold_seq}{For walk-forward results, integer fold sequence to extract,
or \code{"latest"}.}

\item{selection_rationale}{Optional plain-text rationale. Required for
walk-forward results when \code{fold_seq = "latest"}.}

\item{snapshot}{Optional sealed snapshot override for walk-forward results
whose experiment store has moved. The override must have the same
\code{snapshot_id} and \code{snapshot_hash} as the result locator; its \code{db_path} may
differ.}
}
\value{
A \code{ledgr_sweep_candidate} object.
}
\description{
Selects a single row from a sweep result table and packages its params, seed,
and provenance for promotion or inspection.
}
\details{
The returned candidate carries \code{selection_view}, the tibble-like
view passed to \code{ledgr_candidate()}. Promotion-context storage uses that
view to record the filtered/sorted candidate table the user selected from.
\code{ledgr_candidate()} is the supported way to extract params, execution seed,
and row-level provenance for promotion. The selected candidate also carries
the compact reproduction key exposed by
\code{\link[=ledgr_candidate_reproduction_key]{ledgr_candidate_reproduction_key()}}. It avoids making users manually pull
\code{params[[1]]}, \code{execution_seed}, and provenance fields from a tibble row.
}
\section{Articles}{

Exploratory sweeps and promotion:
\code{vignette("sweeps", package = "ledgr")}
\code{system.file("doc", "sweeps.html", package = "ledgr")}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost-model.R
\name{ledgr_cost_spread_bps}
\alias{ledgr_cost_spread_bps}
\alias{ledgr_cost_fixed_fee}
\alias{ledgr_cost_notional_bps_fee}
\alias{ledgr_cost_zero}
\alias{ledgr_cost_chain}
\title{Transaction cost model constructors}
\usage{
ledgr_cost_spread_bps(bps)

ledgr_cost_fixed_fee(amount)

ledgr_cost_notional_bps_fee(bps)

ledgr_cost_zero()

ledgr_cost_chain(...)
}
\arguments{
\item{bps}{Finite non-negative basis-point value.}

\item{amount}{Finite non-negative fixed fee per fill.}

\item{...}{Cost model objects to compose in order.}
}
\value{
A \code{ledgr_cost_model} object.
}
\description{
These constructors create deterministic, classed cost model objects for
ledgr execution. Cost models are experiment-level objects: strategies do not
receive cost state and cost models may not change fill side, quantity,
instrument, or execution timestamp.
Cost identity is stored as \code{cost_plan_json} plus \code{cost_model_hash}; see
\link{ledgr_identity_fields} for how those fields relate to run and candidate
identity.
}
\examples{
spread <- ledgr_cost_spread_bps(5)
fixed <- ledgr_cost_fixed_fee(1)
notional <- ledgr_cost_notional_bps_fee(2)
zero <- ledgr_cost_zero()

cost <- ledgr_cost_chain(spread, fixed, notional)
ledgr_cost_steps(cost)
ledgr_cost_describe(cost)

bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(100, 101, 102),
  low = c(100, 101, 102),
  close = c(100, 101, 102),
  volume = 1000
)
strategy <- function(ctx, params) ctx$flat()
# raises ledgr_cost_model_unspecified: cost_model is required.
try(ledgr_backtest(data = bars, strategy = strategy), silent = TRUE)
bt <- ledgr_backtest(data = bars, strategy = strategy, cost_model = zero)
close(bt)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-dsr.R
\name{ledgr_sweep_cluster}
\alias{ledgr_sweep_cluster}
\alias{as_tibble.ledgr_sweep_cluster}
\alias{print.ledgr_sweep_cluster}
\title{Sweep candidate clustering diagnostic}
\usage{
ledgr_sweep_cluster(sweep, candidates = NULL, distance_threshold = 0.5)

\method{as_tibble}{ledgr_sweep_cluster}(
  x,
  what = c("summary", "membership", "distances"),
  ...
)

\method{print}{ledgr_sweep_cluster}(x, ...)
}
\arguments{
\item{sweep}{A \code{ledgr_sweep_results} object with retained completed returns.}

\item{candidates}{Optional character vector of candidate ids to include.}

\item{distance_threshold}{Numeric scalar in \code{[0, 2]}. Distances are
\code{1 - correlation} over retained return columns.}

\item{x}{A \code{ledgr_sweep_cluster} object.}

\item{what}{Table to return: \code{"summary"}, \code{"membership"}, or
\code{"distances"}.}

\item{...}{Additional arguments passed to tibble printing.}
}
\value{
A \code{ledgr_sweep_cluster} object with \code{summary}, \code{membership},
\code{distances}, and \code{metadata}.
}
\description{
\code{ledgr_sweep_cluster()} clusters retained completed-candidate return series
with one deterministic hierarchical method. The effective independent trial
count is the number of clusters at \code{distance_threshold}; it is evidence for
selection-integrity diagnostics, not a selection rule.
}
\details{
The diagnostic consumes \code{ledgr_sweep_returns_panel(..., value = "returns",
complete = TRUE)}. It does not inspect fills, positions, promotion records, or
walk-forward folds, and it does not mutate sweep artifacts or identity.
}
\examples{
\dontrun{
clusters <- ledgr_sweep_cluster(sweep)
as_tibble(clusters)
as_tibble(clusters, what = "membership")
}
}
\seealso{
\code{vignette("selection-integrity", package = "ledgr")} or
\code{system.file("doc", "selection-integrity.html", package = "ledgr")}.
}

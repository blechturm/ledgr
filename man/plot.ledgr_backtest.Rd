% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.ledgr_backtest}
\alias{plot.ledgr_backtest}
\title{Plot backtest results}
\usage{
\method{plot}{ledgr_backtest}(x, y = NULL, ..., type = "equity")
}
\arguments{
\item{x}{A \code{ledgr_backtest} object.}

\item{y}{Unused.}

\item{...}{Unused.}

\item{type}{Plot type. Only \code{"equity"} is supported in v0.1.2.}
}
\value{
A ggplot object, or a grid object when \code{gridExtra} is installed.
}
\description{
Plot backtest results
}
\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:3,
  instrument_id = "AAA",
  open = c(100, 101, 102, 103),
  high = c(101, 102, 103, 104),
  low = c(99, 100, 101, 102),
  close = c(100, 101, 102, 103),
  volume = 1000
)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- 1
  targets
}
bt <- ledgr_backtest(data = bars, strategy = strategy, initial_cash = 1000)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot(bt)
}
close(bt)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot_adapters.R
\name{ledgr_snapshot_from_df}
\alias{ledgr_snapshot_from_df}
\title{Create a snapshot from an in-memory data.frame}
\usage{
ledgr_snapshot_from_df(
  bars_df,
  instruments_df = NULL,
  db_path = NULL,
  snapshot_id = NULL
)
}
\arguments{
\item{bars_df}{data.frame with required columns: ts_utc, instrument_id,
open, high, low, close. Optional: volume.}

\item{instruments_df}{Optional data.frame with instrument metadata.}

\item{db_path}{Optional DuckDB file path (default: tempfile).}

\item{snapshot_id}{Optional snapshot id (default: v0.1.1 canonical generation).}
}
\value{
A \code{ledgr_snapshot} object.
}
\description{
Validates schema, normalizes timestamps, creates a snapshot via v0.1.1
functions, imports bars, seals the snapshot, and returns a lazy snapshot
object.
}
\section{Articles}{

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars)
ledgr_snapshot_info(snapshot)
ledgr_snapshot_close(snapshot)
}

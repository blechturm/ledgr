% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-tags.R
\name{ledgr_run_tags}
\alias{ledgr_run_tags}
\title{List run tags}
\usage{
ledgr_run_tags(snapshot, run_id = NULL)
}
\arguments{
\item{snapshot}{A sealed \code{ledgr_snapshot} object. Use
\code{ledgr_snapshot_load(db_path, snapshot_id)} to resume from a durable
DuckDB file in a new R session.}

\item{run_id}{Optional run identifier. If supplied, list tags for that run
only.}
}
\value{
A tibble with \code{run_id}, \code{tag}, and \code{created_at_utc}.
}
\description{
Lists mutable run tags stored in a ledgr experiment store. This is a
read-only operation and does not migrate legacy stores.
}
\examples{
bars <- subset(ledgr_demo_bars, instrument_id == "DEMO_01")
snapshot <- ledgr_snapshot_from_df(utils::head(bars, 10))
strategy <- function(ctx, params) ctx$flat()
exp <- ledgr_experiment(snapshot, strategy, opening = ledgr_opening(cash = 1000))
bt <- ledgr_run(exp, params = list(), run_id = "flat")
ledgr_run_tag(snapshot, bt$run_id, "baseline")
ledgr_run_tags(snapshot)
close(bt)
ledgr_snapshot_close(snapshot)
}

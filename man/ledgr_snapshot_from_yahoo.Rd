% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot_adapters.R
\name{ledgr_snapshot_from_yahoo}
\alias{ledgr_snapshot_from_yahoo}
\title{Create a snapshot from Yahoo Finance data (quantmod)}
\usage{
ledgr_snapshot_from_yahoo(
  symbols,
  from,
  to,
  db_path = NULL,
  snapshot_id = NULL,
  ...
)
}
\arguments{
\item{symbols}{Character vector of ticker symbols.}

\item{from}{Start date (character, Date, or POSIXct).}

\item{to}{End date (character, Date, or POSIXct).}

\item{db_path}{Optional DuckDB file path (default: tempfile).}

\item{snapshot_id}{Optional snapshot id (default: v0.1.1 canonical generation).}

\item{...}{Additional arguments passed to \code{quantmod::getSymbols()}.}
}
\value{
A sealed \code{ledgr_snapshot} object.
}
\description{
Fetches historical data from Yahoo Finance via quantmod and delegates to
\code{ledgr_snapshot_from_df()}. The returned handle is already sealed; pass it
directly to \code{ledgr_experiment()} or reopen it later with
\code{ledgr_snapshot_load()}.
}
\details{
quantmod may emit harmless package startup or S3 method-overwrite messages to
stderr while loading its dependencies. Those messages are not ledgr snapshot
warnings. Data availability and content still depend on Yahoo Finance.
}
\section{Articles}{

Durable experiment stores:
\code{vignette("experiment-store", package = "ledgr")}
\code{system.file("doc", "experiment-store.html", package = "ledgr")}
}

\examples{
if (FALSE) {
  # Requires quantmod and network access. Yahoo data can change over time.
  snapshot <- ledgr_snapshot_from_yahoo(
    symbols = c("AAPL", "MSFT"),
    from = "2020-01-01",
    to = "2020-02-01"
  )
  ledgr_snapshot_close(snapshot)
}
}

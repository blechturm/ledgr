% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots-list.R
\name{ledgr_snapshot_load}
\alias{ledgr_snapshot_load}
\title{Load an existing sealed snapshot}
\usage{
ledgr_snapshot_load(db_path, snapshot_id = NULL, verify = FALSE)
}
\arguments{
\item{db_path}{Path to an existing DuckDB file.}

\item{snapshot_id}{Snapshot id to load. The snapshot must exist and be
\code{SEALED}.}

\item{verify}{Logical scalar. If \code{TRUE}, recompute the snapshot hash and
compare it with the stored hash before returning.}
}
\value{
A lazy \code{ledgr_snapshot} object.
}
\description{
Reopens a snapshot that already exists in a DuckDB file. This is the durable
research workflow counterpart to \code{ledgr_snapshot_from_df()}: create and seal a
snapshot once, then load it by path and snapshot id in later R sessions.
}
\details{
\code{ledgr_snapshot_load()} never creates or overwrites snapshots. It only returns
a handle to an existing sealed snapshot. Closing the returned object releases
its DuckDB connection; it does not delete the database file.
}
\examples{
db_path <- tempfile(fileext = ".duckdb")
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = 1000
)
snapshot <- ledgr_snapshot_from_df(bars, db_path = db_path)
snapshot_id <- snapshot$snapshot_id
ledgr_snapshot_close(snapshot)

snapshot <- ledgr_snapshot_load(db_path, snapshot_id, verify = TRUE)
ledgr_snapshot_info(snapshot)
ledgr_snapshot_close(snapshot)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-hash.R
\name{ledgr_data_hash}
\alias{ledgr_data_hash}
\title{Compute deterministic data hash for the legacy bars table (v0.1.0)}
\usage{
ledgr_data_hash(con, instrument_ids, start_ts_utc, end_ts_utc)
}
\arguments{
\item{con}{A DBI connection to DuckDB. This legacy helper does not accept a
data frame directly.}

\item{instrument_ids}{Character vector of instrument ids (order-sensitive).}

\item{start_ts_utc}{Start timestamp (character ISO8601 \code{...Z} or POSIXt).}

\item{end_ts_utc}{End timestamp (character ISO8601 \code{...Z} or POSIXt).}
}
\value{
A SHA-256 hex string.
}
\description{
Computes a deterministic SHA-256 hash over:
\itemize{
\item ordered \code{instrument_ids} (order-sensitive)
\item canonical UTC \code{start_ts_utc} and \code{end_ts_utc} (ISO8601 with trailing \code{Z})
\item a bars fingerprint for the subset ordered by \verb{(instrument_id, ts_utc)}
}
}
\details{
Numeric OHLCV values are rounded to 8 decimal places before hashing.
Missing values are represented as the literal string \code{NA}.

This is a legacy v0.1.0 helper that hashes rows from the raw \code{bars} table or
a compatible temporary view. It remains exported for compatibility with old
low-level workflows, but modern snapshot-backed workflows should use
\code{ledgr_snapshot_from_df()} or \code{ledgr_snapshot_from_csv()} and inspect the
sealed snapshot hash with \code{ledgr_snapshot_info()}.
}
\examples{
db_path <- tempfile(fileext = ".duckdb")
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:2,
  instrument_id = "AAA",
  open = c(100, 101, 102),
  high = c(101, 102, 103),
  low = c(99, 100, 101),
  close = c(100, 101, 102),
  volume = c(1000, 1000, 1000)
)
snapshot <- ledgr_snapshot_from_df(bars, db_path = db_path)
ledgr_snapshot_info(snapshot)$snapshot_hash
ledgr_snapshot_close(snapshot)

if (FALSE) {
  # Legacy v0.1.0 workflows require rows in the raw bars table.
  legacy_path <- tempfile(fileext = ".duckdb")
  con <- ledgr_db_init(legacy_path)
  DBI::dbAppendTable(con, "bars", bars)
  ledgr_data_hash(con, "AAA", "2020-01-01", "2020-01-03")
  DBI::dbDisconnect(con, shutdown = TRUE)
}
}

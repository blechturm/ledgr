% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy-preflight.R
\name{ledgr_strategy_preflight}
\alias{ledgr_strategy_preflight}
\title{Classify a functional strategy's reproducibility tier}
\usage{
ledgr_strategy_preflight(strategy)
}
\arguments{
\item{strategy}{A function with signature \verb{function(ctx, params)}.}
}
\value{
A \code{ledgr_strategy_preflight} object with fields \code{tier}, \code{allowed},
\code{reason}, \code{unresolved_symbols}, \code{package_dependencies}, and \code{notes}.
}
\description{
\code{ledgr_strategy_preflight()} statically inspects a \verb{function(ctx, params)}
strategy before execution. It classifies the strategy into ledgr's
reproducibility tiers:
}
\details{
\itemize{
\item \code{tier_1}: self-contained strategy logic using only explicit \code{params},
base/recommended R references, and ledgr's exported public namespace.
\item \code{tier_2}: inspectable strategy logic that also uses package-qualified
calls outside the active R distribution, such as \code{pkg::fn()}, or resolved
immutable non-function closure objects that ledgr does not store as
standalone replayable artifacts.
\item \code{tier_3}: strategy logic with unresolved free symbols, user helpers,
forbidden nondeterministic calls, or global assignment that ledgr cannot
recover from stored run metadata.
}

The preflight is static analysis, not proof of semantic reproducibility.
Dynamic dispatch, mutable captured environments, and dynamically constructed
code remain user responsibilities.

Tier 3 strategies fail before execution with condition classes
\code{ledgr_strategy_tier3} and \code{ledgr_strategy_preflight_error}; there is no
\code{force = TRUE} override on \code{ledgr_run()} or \code{ledgr_sweep()}. Covered
forbidden calls include direct wall-clock/process-environment calls such as
\code{Sys.time()}, \code{Sys.Date()}, and \code{Sys.getenv()}, visible indirection such as
\code{do.call("Sys.time", list())}, dynamic lookup/evaluation helpers such as
\code{get()}, \code{eval()}, and \code{assign()}, global assignment with \code{<<-}, and visible
context mutation such as \code{attr(ctx, "secret") <- 1}.

Ambient strategy RNG calls such as \code{runif(1)} are Tier 2 under the execution
seed contract for ordinary sequential runs, but they are not certified for
resume or parallel equivalence. Strategies that need pulse-specific stochastic
inputs in those paths should derive them from \code{ctx$pulse_seed} without
reading ambient \code{.Random.seed}. Custom indicator generation has a stricter
deterministic feature contract; do not infer indicator RNG policy from strategy
preflight.
}
\section{Articles}{

Reproducibility model:
\code{vignette("reproducibility", package = "ledgr")}
\code{system.file("doc", "reproducibility.html", package = "ledgr")}
}

\examples{
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets
}
ledgr_strategy_preflight(strategy)
}

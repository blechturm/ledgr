% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{ledgr_backtest}
\alias{ledgr_backtest}
\title{Run a backtest (v0.1.2)}
\usage{
ledgr_backtest(
  snapshot = NULL,
  strategy,
  universe = NULL,
  start = NULL,
  end = NULL,
  initial_cash = 1e+05,
  strategy_params = list(),
  features = list(),
  timing_model = ledgr_timing_next_open(),
  cost_model,
  risk_chain = ledgr_risk_none(),
  fill_model = NULL,
  execution_mode = "audit_log",
  checkpoint_every = 10000L,
  persist_features = TRUE,
  db_path = NULL,
  control = list(),
  run_id = NULL,
  data = NULL
)
}
\arguments{
\item{snapshot}{A \code{ledgr_snapshot} object, or a data frame for the data-first
convenience path.}

\item{strategy}{Strategy function using \verb{function(ctx, params)}, or a
configured strategy list.}

\item{universe}{Character vector of instrument IDs. If \code{NULL}, it is inferred
from the snapshot or data frame.}

\item{start}{Start timestamp (NULL = snapshot start).}

\item{end}{End timestamp (NULL = snapshot end).}

\item{initial_cash}{Starting capital. Must be a finite numeric scalar > 0.}

\item{strategy_params}{JSON-safe list passed to \verb{function(ctx, params)}
strategies and stored as part of run provenance.}

\item{features}{List of ledgr indicator or indicator-bundle definitions
(optional). Bundles flatten into ordinary indicators before runtime.}

\item{timing_model}{Timing model object. Defaults to
\code{ledgr_timing_next_open()}.}

\item{cost_model}{Required ledgr cost model object. Use \code{ledgr_cost_zero()}
for explicit zero-cost execution.}

\item{risk_chain}{Target-risk chain object. Defaults to
\code{ledgr_risk_none()} for explicit no-risk execution.}

\item{fill_model}{Legacy v0.1.8 fill model argument. Supplying it now fails
with \code{ledgr_legacy_fill_model_shape}; use \code{timing_model} plus
\code{cost_model}.}

\item{execution_mode}{Execution mode ("db_live" or "audit_log").}

\item{checkpoint_every}{Flush interval for audit_log mode.}

\item{persist_features}{If FALSE, skip persisting per-pulse features to DuckDB.}

\item{db_path}{Database path for the run ledger (NULL = snapshot DB).}

\item{control}{Optional list of engine overrides (e.g., execution_mode).}

\item{run_id}{Optional run identifier to resume or reuse.}

\item{data}{Optional data frame/tibble or \code{ledgr_snapshot}. Exactly one of
\code{snapshot} and \code{data} may be supplied.}
}
\value{
A \code{ledgr_backtest} object.
}
\description{
Thin wrapper around the canonical engine path.
}
\details{
v0.1.7 introduces the experiment-first public workflow:
\code{ledgr_experiment()} plus \code{ledgr_run()}. \code{ledgr_backtest()} remains available
as a compatibility wrapper around the same canonical runner path.

Strategies return target holdings. The default timing model is \code{next_open}: a
target decided at pulse \code{t} is filled at the next available bar. Targets on
the final pulse therefore cannot be filled unless another bar exists after
\code{end}.

Cost models are explicit. Use \code{ledgr_cost_zero()} for no-cost execution.
\code{ledgr_cost_spread_bps()} treats \code{spread_bps} as a quoted bid/ask spread:
buys cross half the spread above the reference price and sells cross half the
spread below it, so a round trip crosses approximately \code{spread_bps} basis
points before explicit fees.

v0.1.x does not provide a supported broker-style short-selling contract.
Strategy authors should treat negative target quantities as outside the
supported public workflow until explicit shorting semantics are specified.
}
\section{Articles}{

Strategy authoring:
\code{vignette("strategy-development", package = "ledgr")}
\code{system.file("doc", "strategy-development.html", package = "ledgr")}

Metrics and accounting:
\code{vignette("metrics-and-accounting", package = "ledgr")}
\code{system.file("doc", "metrics-and-accounting.html", package = "ledgr")}
}

\examples{
bars <- data.frame(
  ts_utc = as.POSIXct("2020-01-01", tz = "UTC") + 86400 * 0:3,
  instrument_id = "AAA",
  open = c(100, 101, 102, 103),
  high = c(101, 102, 103, 104),
  low = c(99, 100, 101, 102),
  close = c(100, 101, 102, 103),
  volume = 1000
)
strategy <- function(ctx, params) {
  targets <- ctx$flat()
  targets["AAA"] <- if (ctx$close("AAA") > 100) 1 else 0
  targets
}
bt <- ledgr_backtest(
  data = bars,
  strategy = strategy,
  initial_cash = 1000,
  cost_model = ledgr_cost_zero()
)
print(bt)
close(bt)
}

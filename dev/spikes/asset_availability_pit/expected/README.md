# Independent Expected Tables

Create `W01.csv` through `W31.csv` using the long schema in
`../witness_spec.md`. Calculate the values independently of the fold fork and
all representation providers.

Every table requires maintainer approval and review by an author who did not
execute the spike. Changes after freeze require a versioned rationale and a
rerun of every affected provider.

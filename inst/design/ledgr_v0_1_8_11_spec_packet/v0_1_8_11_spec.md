# ledgr v0.1.8.11 Spec

**Status:** Active packet; Batch 9 complete after Claude review and LDG-2539 next.
**Target Branch:** `v0.1.8.11`.
**Scope:** Documentation, structure, and cleanup release before v0.1.9
feature work. Reduce repository entropy after the v0.1.8.x optimization,
substrate, and B2 spot-FIFO arc by making the architecture discoverable,
auditing stale contract surfaces, refreshing user-facing documentation, and
building internal maintainer documentation.
**Ticket state:** Tickets are cut in `v0_1_8_11_tickets.md` and `tickets.yml`.
Batch 0 packet alignment, LDG-2528 `contracts.md` audit, LDG-2529 planning
housekeeping, and LDG-2530 RFC decision-index work are complete after Claude
review. LDG-2531 `contracts.md` structure work is complete after Claude
review. LDG-2532 maintainer-manual foundation work is complete after Claude
review. LDG-2533 user-facing documentation refresh work is complete after
Claude review. LDG-2534 performance arc narrative work is complete after Claude
review. LDG-2535 research software disclaimer surface work is complete after
Claude review. LDG-2536 generated docs and man-page audit work is complete
after Claude review. LDG-2539 was added to consume the generated-doc audit
findings. Substantive work continues with LDG-2539, generated docs
stale-language cleanup.
**Non-scope for this pass:** new public API, execution-semantics changes,
event-schema changes, durable identity byte changes, default compiled
execution, durable compiled integration, non-spot compiled accounting, target
risk, walk-forward execution, public cost/liquidity APIs, OMS or paper/live
semantics, broad implementation refactors, public benchmark marketing, and
any feature implementation not explicitly converted into a reviewed ticket.

---

## 0. Source Inputs

Authoritative inputs:

- `inst/design/contracts.md`
- `inst/design/README.md`
- `inst/design/ledgr_roadmap.md`
- `inst/design/horizon.md`
- `inst/design/rfc_cycle.md`
- `inst/design/vignette_styleguide.md`
- `inst/design/release_ci_playbook.md`
- `inst/design/ledgr_v0_1_8_11_spec_packet/v0_1_8_11_tickets.md`
- `inst/design/ledgr_v0_1_8_11_spec_packet/tickets.yml`
- `inst/design/ledgr_v0_1_8_11_spec_packet/batch_plan.md`

Completed packet inputs:

- `inst/design/ledgr_v0_1_8_5_spec_packet/`
- `inst/design/ledgr_v0_1_8_7_spec_packet/`
- `inst/design/ledgr_v0_1_8_8_spec_packet/`
- `inst/design/ledgr_v0_1_8_9_spec_packet/`
- `inst/design/ledgr_v0_1_8_10_spec_packet/`

Key architecture, review, and benchmark inputs:

- `inst/design/maintainer_review/fold_core_workbook.qmd`
- `inst/design/maintainer_review/feature_value_path_workbook.qmd`
- `inst/design/architecture/fold_core_trust_boundary.md`
- `inst/design/collapse_optimization_map.md`
- `dev/bench/README.md`
- `dev/bench/peer_benchmark/peer_benchmark.md`
- `inst/design/ledgr_v0_1_8_10_spec_packet/per_lane_attribution.md`

Accepted RFC and ADR inputs:

- `inst/design/adr/0005-b2-spot-fifo-scope-guard.md`
- `inst/design/adr/0004-dependency-footprint-and-strategy-interface.md`
- `inst/design/rfc/rfc_sweep_single_core_optimization_routes_v0_1_8_synthesis.md`
- `inst/design/rfc/rfc_research_workflow_artifact_topology_v0_1_8_x_synthesis.md`
- `inst/design/rfc/rfc_feature_projection_shape_and_lookback_v0_1_8_x_synthesis.md`
- `inst/design/rfc/rfc_grid_level_feature_artifacts_wide_runtime_views_v0_1_8_x_synthesis.md`
- `inst/design/rfc/rfc_strategy_callback_contract_addendum_v0_1_8_10_synthesis.md`
- `inst/design/rfc/rfc_strategy_callback_contract_addendum_v0_1_8_10_final_review.md`
- `inst/design/rfc/rfc_strategy_authoring_helpers_v0_1_8_x_synthesis.md`
- `inst/design/rfc/rfc_strategy_authoring_helpers_v0_1_8_x_final_review.md`
- `inst/design/rfc/rfc_compiled_hot_frame_b2_v0_1_9_x_synthesis.md`
- `inst/design/rfc/rfc_public_transaction_cost_model_api_v0_1_9_x_synthesis.md`
- `inst/design/rfc/rfc_chainable_risk_oms_policy_boundary_synthesis.md`

Horizon entries promoted into this packet:

- `2026-06-02 [documentation] Documentation, structure, and cleanup release before v0.1.9 features`
- `2026-05-30 [documentation] Maintainer manual article backlog after v0.1.8.8 skeleton`
- `2026-06-01 [documentation] User-facing research-software disclaimer for financial backtesting`
- `2026-06-01 [strategy] Strategy callback contract + authoring helpers post-v0.1.8.x direction`
- `2026-06-02 [architecture] B2 spot-FIFO accelerator is not a derivatives accounting model`
- v0.1.8.x benchmark / performance-attribution horizon closeouts that need a
  synthesized internal narrative rather than public speed claims.

---

## 1. Thesis

v0.1.8.10 closed the v0.1.8.x single-core arc. The codebase now has a deeper
and better-ratified architecture: matrix-canonical strategy accessors,
fold-owned FIFO accounting, scoped public memory-backed B2 spot-FIFO opt-in,
parallel sweep dispatch, canonical JSON v2, durable event evidence, and
phase-separated benchmark reporting.

The governance worked. The problem is discoverability. Important decisions now
live across spec packets, RFC syntheses, final reviews, horizon entries,
benchmark notes, and contracts. A maintainer or agent can still recover the
truth, but doing so requires too much archaeology.

v0.1.8.11 is the entropy-management release before v0.1.9 target risk. It
should turn ratified decisions into a navigable architecture surface without
changing execution semantics.

---

## 2. Release Goals

v0.1.8.11 has nine planning goals:

1. Audit `contracts.md` for stale, missing, duplicated, or poorly organized
   contract language after v0.1.8.10.
2. Restructure or annotate `contracts.md` by surface where appropriate:
   execution spec, fold engine, output handlers, event evidence, accounting,
   strategy context, sweep, feature projection, persistence, documentation, and
   release gates.
3. Build a discoverable RFC / decision index that tells readers which accepted
   documents carry load-bearing decisions and which are historical scaffolding.
4. Populate ADRs for stabilized architectural decisions that currently live
   only in accepted RFCs or packet closeouts.
5. Grow the internal maintainer manual from the v0.1.8.8 skeleton into coherent
   articles. Manual articles may be reviewed in batches; partial completion is
   acceptable if the packet records a bounded remainder for the v0.1.9.x
   follow-on documentation slot. Priority order is execution / fold core,
   observability / determinism, sweep, snapshots/data, features, then benchmark
   methodology.
6. Refresh user-facing documentation for the post-v0.1.8.10 reality:
   who-ledgr-is-for, why R, strategy development, research workflow,
   reproducibility, sweep/B2 opt-in boundaries, and benchmark interpretation.
7. Add a plain-English financial research-software disclaimer surface and link
   it from the appropriate user-facing entry points.
8. Synthesize the v0.1.8.7-v0.1.8.10 performance arc internally: what got
   faster, where the measurements came from, how ledgr differs from peers, and
   which claims are intentionally not public marketing.
9. Housekeep `horizon.md`, the roadmap, design index, and agent notes so active
   and resolved entries reflect the v0.1.8.10 release and v0.1.8.11 planning
   state.

The release succeeds when a new contributor can answer "what is the current
architecture and where are the binding decisions?" without reading every RFC in
chronological order.

---

## 3. Binding Boundaries

### 3.1 Documentation Is Not Execution Scope

This packet does not authorize execution behavior changes. If the audit finds a
real contract bug, the correct outcome is a routed finding and a later ticket,
not an incidental implementation fix.

Allowed implementation side effects are narrow:

- stale-link repair;
- stale diagram correction;
- generated-documentation guard updates;
- doc test updates;
- typo / wording fixes that do not alter semantics;
- small helper scripts for documentation inventory or audit reports.

### 3.2 Contracts Audit Scope

The `contracts.md` audit should answer:

- Does every current public execution surface have a contract entry?
- Are removed or retired concepts still described as current behavior?
- Does the document distinguish canonical evidence from derived outputs?
- Does it reflect the post-v0.1.8.10 B2 scope guard:
  `compiled_accounting_model = NULL | "spot_fifo"`?
- Does it reflect that durable compiled integration remains deferred?
- Does it keep `ledgr_run()` and `ledgr_sweep()` on one shared fold core?
- Does it correctly separate strategy context, fill context, cost policy,
  target risk, liquidity, OMS, and future paper/live semantics?
- Is documentation-discovery behavior still current?

The audit may propose structural reorganization. It must not silently weaken
existing contracts.

### 3.3 RFCs Remain Authoritative

v0.1.8.11 synthesis should summarize and index accepted RFC decisions. It
should not rewrite accepted RFC text into replacement authority. The source
documents remain binding until superseded by a later spec, contract update,
ADR, or architecture note.

### 3.4 B2 Language Guard

B2 is a scoped spot-asset FIFO fill-batch accelerator for memory-backed sweeps.
Documentation must not describe it as:

- a general compiled fold core;
- default execution;
- durable compiled integration;
- a derivatives, margin, or non-spot accounting model;
- a replacement for event-sourced evidence.

### 3.5 Benchmark Language Guard

v0.1.8.x benchmark material is local, current-source, machine-specific
engineering evidence unless a future packet explicitly authorizes a public
claim. The v0.1.8.11 performance narrative is internal-facing first.

The teaching surface should live under `inst/design/manual/`, with raw
evidence, records, and measurement notes remaining under `dev/bench/`. The
manual article should link to the benchmark artifacts rather than copying every
number. The scoped narrative window is v0.1.8.7 through v0.1.8.10: Optimization
Round 2, parallel / diagnostic setup, v0.1.8.9 single-core optimization, and
v0.1.8.10 substrate / B2 closeout.

### 3.6 No Pass 2 Strategy Helpers

v0.1.8.11 may document the strategy accessor surface that shipped in
v0.1.8.10: `ctx$vec`, `ctx$idx()`, and
`ctx$vec$feature(feature_id)`. It must not add Pass 2 public strategy-helper
extensions. Rank-weight, inverse-vol, normalization, rebalance-band,
target-diagnostic, long-short, levered, hedged, cost-aware, or larger
declarative strategy constructors remain deferred to the v0.1.9.x helper
milestone or later RFCs.

### 3.7 Audit Artifact Homes

Review should confirm these paths before tickets are cut:

- `inst/design/ledgr_v0_1_8_11_spec_packet/contracts_audit.md` for the
  `contracts.md` audit report.
- `inst/design/ledgr_v0_1_8_11_spec_packet/generated_docs_audit.md` for the
  generated-docs / man-page audit and routed stale-language findings.
- `inst/design/rfc/README.md` or
  `inst/design/ledgr_v0_1_8_11_spec_packet/rfc_decision_index.md` for the RFC /
  decision index. The final home should be chosen before the ticket is cut; if
  the index is meant to stay evergreen, prefer `inst/design/rfc/README.md`.
- `inst/design/manual/performance_arc_v0_1_8_x.qmd` for the teaching narrative,
  with measurement records staying under `dev/bench/`.

The generated-docs / man-page audit output is a routed report: it should list
stale or suspect help-page language, classify each item as fix-now,
defer-with-reason, or no-action, and name the files/tickets that consume the
finding.

---

## 4. Planned Workstreams

Tickets are cut in `v0_1_8_11_tickets.md`. The planned workstreams are:

1. **Contracts audit and structure pass.** Produce `contracts_audit.md` first,
   then edit `contracts.md` only after findings are routed.
2. **Design index and roadmap cleanup.** Make active packet state and completed
   packet references current.
3. **Horizon housekeeping.** Move shipped v0.1.8.10 ideas out of open planning;
   keep future feature work parked with correct milestones.
4. **RFC / decision index.** Add a reader-oriented map from topic to binding
   RFC / ADR / packet. Decide whether this is an evergreen
   `inst/design/rfc/README.md` or packet-local `rfc_decision_index.md` before
   cutting the ticket.
5. **ADR population.** Identify decisions stable enough for ADR extraction.
6. **Maintainer manual articles.** Author internal prose under
   `inst/design/manual/`. Manual sources are Quarto files rendered to sibling
   GitHub-flavored Markdown for repository browsing.
7. **User-facing documentation refresh.** Update vignettes and README surfaces
   affected by the post-v0.1.8.10 architecture. Required first-pass articles:
   strategy development, research workflow, who-ledgr-is-for, and why R.
8. **Disclaimer surface.** Add `DISCLAIMER.md` and modest links if approved.
9. **Performance narrative.** Create an internal v0.1.8.7-v0.1.8.10
   performance arc article with careful benchmark caveats.
10. **Generated docs / man-page audit.** Produce `generated_docs_audit.md`;
    identify stale help-page language and route fixes.
11. **inst/ subdirectory audit.** Produce `inst_audit.md`; inventory and route
    stale or unreferenced files in `inst/design/architecture/`,
    `inst/design/maintainer_review/`, `inst/diagrams/`, `inst/examples/`,
    `inst/schemas/`, and `inst/testdata/`. Preserve the binding architecture
    paths (`fold_core_trust_boundary.md`, `ledgr_v0_1_8_sweep_architecture.md`)
    in place.

---

## 5. Non-Goals

- No target-risk implementation.
- No walk-forward implementation.
- No cost/liquidity public API implementation.
- No OMS, broker, paper/live, or external-provider work.
- No compiled-core promotion.
- No default B2 execution.
- No durable compiled B2 integration.
- No non-spot compiled accounting.
- No broad refactor while writing docs.
- No public hosted benchmark dashboard.
- No package-vignette benchmark marketing.
- No substantive work outside the reviewed tickets; after Batch 5 review,
  continue with LDG-2533.

---

## 6. Review Questions

1. Is `v0.1.8.11` the right version number and packet shape for this entropy
   management cycle?
2. Confirmed review default: the `contracts.md` audit should be a standalone
   report before any contract edits. Is `contracts_audit.md` in the packet
   directory the right home?
3. Resolved in LDG-2530: B2 spot-FIFO scope guard became ADR-0005; canonical R
   default execution, matrix-canonical strategy accessor contract, and
   fold-owned FIFO accounting boundary remain routed through RFC / contract /
   packet authority unless a future RFC reopens them.
4. Required first-pass user-facing refresh set is strategy development,
   research workflow, who-ledgr-is-for, and why R. Are any of those deferrable?
5. Review default: `DISCLAIMER.md` belongs in the first ticket batch because it
   is small and cleanly coupled to the documentation refresh. Confirm or defer.
6. Review default: performance teaching surface lives in
   `inst/design/manual/performance_arc_v0_1_8_x.qmd`; raw evidence stays under
   `dev/bench/`. Confirm this split.
7. Should this stay one v0.1.8.11 release, or split into v0.1.8.11
   audit/housekeeping/contracts/index and v0.1.8.12
   manual/vignettes/narrative/disclaimer if the ticket list becomes too large?

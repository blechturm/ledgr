# Examples

This directory is not a user-facing first-run path.

Use the installed vignettes for runnable workflows:

```r
vignette(package = "ledgr")
```

The maintained first-run path is `vignette("getting-started", package =
"ledgr")`; exploratory sweeps are covered in `vignette("sweeps", package =
"ledgr")`.

